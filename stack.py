# lifo -use list obkj as stack
browser_session = []
browser_session.append(1)
browser_session.append(2)
browser_session.append(3)

# print(browser_session)
# last = browser_session.pop()
browser_session.pop()
# print(last)
# print(browser_session)
# print("redirect", browser_session[-1])

if not browser_session:
    # print(redirect)
    browser_session[-1]
