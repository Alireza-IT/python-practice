def multiply(*numbers):
    total = 1
    for number in numbers:
        total *= number
    return total


print('start')  # f9 for breakpoint f5 for run application
print(multiply(1, 2, 3))

# shift + f11to go to step out of funtuction
# fun + arrow key to go to the first and rnd of line and page
# move line =>select line and oprion anad arrow key
# multiply => mty
