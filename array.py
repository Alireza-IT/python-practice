from array import array
# use array instead of list for performance for large squence numbers and couter situations
numbers = array("i", [1, 2, 3])  # type code of element
# every obj should be integer as we defined
[1, 2, 3]
